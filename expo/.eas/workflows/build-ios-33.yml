name: Build iOS 1.0.0 (33) (paused)

# Trigger disabled after Build 33 was requested to prevent duplicate builds.

jobs:
  build_ios:
    name: Build iOS 1.0.0 (33)
    type: build
    params:
      platform: ios
      profile: production
