name: Build iOS Apple Review Cleanup

on:
  push:
    branches: ['fix/apple-guideline-5-6-cleanup']

jobs:
  build_ios:
    name: Build iOS Apple Review Cleanup
    type: build
    params:
      platform: ios
      profile: production
