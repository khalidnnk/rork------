name: Build iOS widget permission UI

on:
  push:
    branches: ['feature/v1.3.4-location-widget-ui']

jobs:
  build_ios:
    name: Build iOS widget permission UI
    type: build
    params:
      platform: ios
      profile: production
