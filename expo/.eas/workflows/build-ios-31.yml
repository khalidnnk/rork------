name: Build iOS 1.0.0 (31)

on:
  push:
    branches: ['feature/v1.3.4-location-widget-ui']

jobs:
  build_ios:
    name: Build iOS 1.0.0 (31)
    type: build
    params:
      platform: ios
      profile: production
