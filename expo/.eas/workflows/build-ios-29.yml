name: Build iOS 1.0.0 (29) (paused)

# Paused while EAS background operations are unavailable.

jobs:
  build_ios:
    name: Build iOS 1.0.0 (29)
    type: build
    params:
      platform: ios
      profile: production
