name: Submit iOS build 28 (disabled)

# Retained for build history only. No automatic trigger.

jobs:
  submit_ios:
    name: Upload build 28 to App Store Connect
    type: submit
    params:
      build_id: 5f13ce4b-e6d9-466f-9973-6bde87856da4
      profile: production
