name: Build iOS 1.0.0 (28)

on:
  push:
    branches: ['feature/v1.3.4-location-widget-ui']

jobs:
  build_ios:
    name: Build iOS 1.0.0 (28)
    type: build
    params:
      platform: ios
      profile: production
